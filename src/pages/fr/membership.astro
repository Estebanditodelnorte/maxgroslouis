---
import Layout from '../../layouts/Layout.astro';

const niveaux = [
  {
    nom: 'Paiement d’adhésion',
    montant: '60 $ / an',
    avantages: [
      'Des frais de 60 $ sont exigés annuellement pour renouveler l’adhésion. Veuillez effectuer votre paiement rapidement et conserver la preuve pour vos dossiers.',
    ],
    ctaLabel: 'Paypal',
    ctaHref: '/fr/contact',
  },
  {
    nom: 'Donner de votre temps',
    montant: 'Gratuit',
    avantages: [
      'Nous accueillons les personnes qui souhaitent offrir leur temps ou expertise afin de soutenir la mission du musée. Contactez-nous pour connaître les besoins actuels en bénévolat.',
    ],
    ctaLabel: 'Nous écrire',
    ctaHref: '/fr/contact',
  },
  {
    nom: 'Faire un don',
    montant: 'Montant au choix',
    avantages: [
      'Un reçu fiscal peut être émis pour les montants de 20 $ et plus. Merci de conserver la preuve de paiement pour vos dossiers.',
    ],
    ctaLabel: 'Faire un don',
    ctaHref: '/fr/contact',
  },
];
---

<Layout title="Adhésion | Espace Max Gros-Louis">
  <section class="membership-hero glass-panel">
    <div>
      <p class="eyebrow">Soutenir la vision</p>
      <h1>Votre contribution garde la culture wendate bien vivante.</h1>
      <p>
        Les adhésions et les dons nous aident à préserver, documenter et partager l’héritage du Grand Chef Max Gros-Louis. Chaque adhésion soutient
        la conservation des artefacts, la numérisation, les programmes éducatifs et les activités intergénérationnelles qui renforcent la continuité culturelle.
      </p>
      <a href="/fr/contact" class="btn primary">Parler avec notre équipe</a>
    </div>
    <img src="/images/webp/community-collection.webp" alt="Visiteurs explorant des sculptures et des sculptures sur bois" loading="lazy" />
  </section>

  <section class="tier-grid">
    {niveaux.map((niveau) => (
      <article class="tier glass-panel">
        <h2>{niveau.nom}</h2>
        <p class="amount">{niveau.montant}</p>
        <ul>
          {niveau.avantages.map((avantage) => (
            <li>{avantage}</li>
          ))}
        </ul>
        <a class="btn ghost" href={niveau.ctaHref ?? '/fr/contact'}>{niveau.ctaLabel ?? 'Contactez-nous'}</a>
      </article>
    ))}
  </section>

  <section class="impact glass-panel">
    <div>
      <p class="eyebrow">Impact</p>
      <h2>Où voyagent vos contributions.</h2>
    </div>
    <div class="impact-grid">
      <article>
        <span class="stat">42 %</span>
        <p>Inventaire des collections, numérisation et conservation préventive.</p>
      </article>
      <article>
        <span class="stat">31 %</span>
        <p>Recherche et développement des expositions.</p>
      </article>
      <article>
        <span class="stat">27 %</span>
        <p>Administration et rayonnement.</p>
      </article>
    </div>
  </section>
</Layout>

<style>
  section {
    margin-top: clamp(2rem, 6vw, 4rem);
  }

  .membership-hero {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(240px, 1fr));
    gap: clamp(1.5rem, 5vw, 4rem);
    align-items: center;
  }

  .membership-hero img {
    width: 100%;
    border-radius: 1.25rem;
    object-fit: cover;
  }

  .tier-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
    gap: 1.25rem;
  }

  .tier ul {
    list-style: none;
    padding-left: 0;
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
  }

  .amount {
    font-size: 1.25rem;
    color: var(--global-accent-color);
    letter-spacing: 0.1em;
  }

  .impact-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(180px, 1fr));
    gap: 1rem;
  }
</style>
